{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "allowJs": true
  },
  "include": [
    "global.d.ts", // <-- ensure your declarations file is picked up
    ".",         // or ".", if your code lives at the root
    "**/*.ts",
    "**/*.tsx"
  ]
}

